PySide6>=6.6
# Note: QtWebEngine ships inside the PySide6 meta package (via
# PySide6-Addons). There is no separate "PySide6-WebEngine" wheel
# on PyPI — listing one breaks `pip install -r`.
# Auth/protocol stack for the new docs-nestedModel sync. gkeepapi is kept
# as a fallback for one release (set keep_protocol_v2:false in config).
gkeepapi>=0.15
gpsoauth>=1.0
keyring>=24
requests>=2.31
